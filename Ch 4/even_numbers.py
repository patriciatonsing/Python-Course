# convert the results of range directly into a list using the list() function
# even numbers from 2 adding 2 up to 11
even_numbers = list(range(2, 11, 2))
print(even_numbers)

odd_numbers = list(range(3, 21, 2))
print(odd_numbers)
